/**
 * @authbound/core
 *
 * Framework-agnostic core SDK for Authbound identity and age verification.
 * Provides shared types, schemas, and utilities used across all Authbound packages.
 *
 * @example
 * ```ts
 * import { VerificationStatus, AssuranceLevel, calculateAge } from '@authbound/core';
 * ```
 */

// Export all types and schemas
export * from "./types";
