{
  "name": "@authbound/issuer-agent-basic",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @authbound/server build",
    "dev": "node server.mjs",
    "check": "node --check server.mjs",
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "@authbound/server": "workspace:*"
  }
}
